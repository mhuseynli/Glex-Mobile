export default function () {
  return {
    // * For pages which has different header with a title
    pageTitle: null,

    // * For pages which have a button on their footer
    buttonFooter: false,
  };
}
